[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "dotnet Clinic-System.API.dll"
healthcheckPath = "/swagger/index.html"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
